<table class="table table-striped">
    <thead>
    <tr>
        <th>Name</th>
        <th>Surname</th>
        <th>Job</th>
        <th>Wage</th>
        <th>Room</th>
        {{#isadmin}}
        <th>Actions</th>
        {{/isadmin}}
    </tr>
    </thead>

    <tbody>
    {{# employees }}
        <tr>
            <td>{{ name }}</td>
            <td>{{ surname }}</td>
            <td>{{ job }}</td>
            <td>{{ wage }}</td>
            <td>{{ room }}</td>
            <td>
            {{#isadmin}}
                <form method="post" action="updateEmployee.php">
                    <input type="hidden" name="employee_id" value="{{employee_id}}" />
                    <input type="submit" value="Update" class="btn btn-info"/>
                </form>
                <form method="post" action="deleteEmployee.php" onsubmit="return confirm('Really delete room?\nAction cannot be undone!')">
                    <input type="hidden" name="employee_id" value="{{employee_id}}" />
                    <input type="submit" value="Delete" class="btn btn-danger"/>
                </form>
            {{/isadmin}}
            </td>
        </tr>
    {{/ employees }}
    </tbody>
</table>
{{#isadmin}}
<a href="insertEmployee.php" class="btn btn-primary">New Employee</a>
<a href="insertKey.php" class="btn btn-primary">Add Key</a>
<a href="deleteKey.php" class="btn btn-primary">Delete Key</a>
{{/isadmin}}
<a href="logout.php" class="btn btn-primary">Log out</a>
<a href="prohlizec.php" class="btn btn-primary">Room list</a>

